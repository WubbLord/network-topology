\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
% The preceding line is only needed to identify funding in the first footnote. If that is unneeded, please comment it out.
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\graphicspath{{results/figs/}{report/results/figs/}}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}

\title{Impact of Network Topology on Distributed Inference:\\
Circulant Graphs as a Drop-In Replacement for 3D Torus Interconnects}

\author{
\IEEEauthorblockN{Nikhil Vemuri}
\IEEEauthorblockA{nvemuri@mit.edu}
\and
\IEEEauthorblockN{Brian Zhang}
\IEEEauthorblockA{zhangbr@mit.edu}
}

\maketitle

% NOTE: 4-page limit, not including references and the ``LLM Usage'' section.
% 5 pages are allowed for the graduate version of the class.

\section{Introduction}
Modern large-model inference is no longer a single-accelerator problem. Serving
models at and beyond GPT-3 scale, including mixture-of-experts (MoE) layers,
requires distributing tensors across many accelerator chips, which turns the
inter-chip network into a first-order performance constraint. Google's TPU v4
systems use a 3D torus with six inter-chip interconnect links per chip, but the
reason this topology is a good fit for inference communication is not obvious:
the best topology depends on which collective operations the mapper induces, how
those collectives load physical links, and whether the mapping itself changes
once realistic network costs are included.

This project studies the impact of network topology on distributed inference by
coupling AccelForge's mapping flow with a topology-aware interconnect model. We
model a 64-chip TPU-v4-like system and compare rings, meshes, tori, and
circulant graphs under comparable hardware budgets. The mapper first selects a
spatial partitioning using a logical
\texttt{NetworkMemory} level. We then extract the resulting inter-chip tensor
traffic, infer collective operations such as AllReduce, ReduceScatter,
AllGather, Broadcast, and point-to-point transfers, and replay those transfers
on candidate physical topologies using link-level bottleneck costs. The measured
network costs are fed back into the mapper until the proxy costs stabilize.

Our main proposal is the circulant graph $C(64,\{1,5,17\})$ as a drop-in
replacement for a 64-chip 3D torus with the same six-link-per-chip budget. Each
generator forms an edge-disjoint Hamiltonian cycle, allowing AllReduce traffic
to be split across three full-chip rings with no shared directed links. This
structure reduces the AllReduce bottleneck-link load by $2.29\times$ relative
to the 3D torus's dimension-wise ring schedule, while also reducing graph
diameter from 6 to 4 hops.

We evaluate this design across unbatched matrix multiplications, batched matrix
multiplications, a decomposed GPT-3 175B inference workload, direct transformer
stress tests, and synthetic MoE token dispatch/combine workloads. Three findings
emerge. First, topology choice alone causes multi-fold latency differences:
the circulant graph wins all completed AccelForge-backed matmul, batched
matmul, and decomposed GPT-3 runs, with the GPT-3 run showing $1.80\times$
lower network latency than the 3D torus and $3.03\times$ lower latency than the
3D mesh. Second, the benefit comes from bottleneck-link load rather than
first-byte latency; serialization dominates at the message sizes generated by
these workloads. Third, topology-aware feedback changes the estimated network
cost substantially but has low mapping sensitivity in our completed runs: the
feedback loop converges in two iterations and the inferred collective plan is
stable from the first to final mapping.


\section{Related Work}
TPU v4 motivates our baseline because it uses a 3D torus-style inter-chip
interconnect for large accelerator pods \cite{tpuv4}. Prior distributed deep
learning systems show that transformer inference and training rely heavily on
collectives such as AllReduce, especially under tensor parallelism
\cite{megatron}. MoE models add sparse all-to-all token dispatch and combine
traffic \cite{gshard,switch}. Circulant and multi-loop networks have also been
studied as low-degree interconnection networks \cite{circulant_survey}; our
work differs by evaluating such graphs inside an accelerator mapping loop
rather than only as abstract graph families.

\section{Design}
\subsection{Topology Cost Model}
The network model represents each topology as a graph with TPU-v4-like links:
45 GB/s unidirectional bandwidth, 4 pJ/bit/hop energy, and 500 ns per hop. A
transfer is routed onto directed physical links. Energy is the total bit-hop
cost,
\begin{equation}
E = 8 \alpha \sum_{\ell} B_{\ell},
\end{equation}
where $B_{\ell}$ is the byte load on link $\ell$ and $\alpha$ is the
energy-per-bit-per-hop. Latency is the bottleneck serialization time plus a
first-byte hop term,
\begin{equation}
L = \max_{\ell} B_{\ell}/\beta + D\lambda,
\end{equation}
where $\beta$ is link bandwidth, $D$ is graph diameter, and $\lambda$ is
per-hop latency. For the matmul and GPT-3 experiments, collectives are costed
independently and summed in workload order. For MoE, all point-to-point
transfers inside one dispatch or combine phase are merged before costing,
because those sparse sends are logically concurrent.

Topology-specific routing is important. Rings use the standard ring AllReduce.
Tori use independent per-dimension ring collectives. Meshes use linear
pipelines because they do not have wraparound links. Generic graphs fall back to
shortest paths, while the proposed circulant graph uses its Hamiltonian cycles
for full-chip AllReduce.

\subsection{AccelForge Feedback Loop}
AccelForge maps a workload onto a chip hierarchy whose outer level is a logical
\texttt{NetworkMemory}. That level initially has HBM-like per-byte proxy costs.
After mapping, we read the mapper's \texttt{NetworkMemory} accesses, identify
which tensor ranks were sharded across chips, infer the required collective, and
evaluate that collective on the physical topology. The resulting per-byte read
and write costs are fed back into \texttt{NetworkMemory}. This repeats until the
largest proxy change is below 5\% or six iterations are reached. In the
completed runs, every successful topology/workload pair converged in two
iterations.

\subsection{Circulant Topology}
The proposed topology is $C(64,\{1,5,17\})$: chip $i$ connects to
$(i \pm g) \bmod 64$ for each generator $g$. Since all three generators are
coprime to 64, each generator forms a Hamiltonian cycle over all chips. These
cycles are edge-disjoint, so AllReduce splits data into three chunks and runs
three ring AllReduces in parallel. For message size $B$, each circulant
directed link carries
\[
2(64-1)B/(3\cdot 64) \approx 0.66B,
\]
while a $4\times4\times4$ torus dimension carries $2(4-1)B/4 = 1.5B$.
This predicts a $1.5/0.66=2.29\times$ AllReduce latency advantage before
considering the circulant's smaller diameter.

\begin{table}[t]
\caption{Core 64-chip topology properties in the model.}
\label{tab:topologies}
\centering
\small
\begin{tabular}{lccc}
\hline
Topology & Degree & Diameter & Avg. hops \\
\hline
Ring & 2 & 32 & 16.25 \\
Mesh $4^3$ & 3--6 & 9 & 3.81 \\
Torus $4^3$ & 6 & 6 & 3.05 \\
Circulant $\{1,5,17\}$ & 6 & 4 & 2.73 \\
\hline
\end{tabular}
\end{table}

\section{Experiment Setup}
We evaluate a 64-chip TPU-v4-like system. Most AccelForge experiments map on
8 chips for tractability and scale extracted network traffic by $64/8=8$ before
topology replay. We also run a direct 64-chip mapping sweep for several large
matmuls to test that this shortcut does not change the qualitative conclusion.

The AccelForge-backed workloads are: seven unbatched matmuls, twenty-one
batched matmuls using the same shapes at batch sizes 256, 512, and 1024, a
decomposed GPT-3 175B inference workload with ten single-einsum components
replicated over 96 layers, and four large matmuls mapped directly with
\texttt{MAP\_CHIPS=64}. We additionally use a direct transformer stress model
for tensor-parallel, data-parallel, and pipeline-parallel traffic patterns, and
a synthetic MoE model with dispatch and combine phases under clustered, spread,
and topology-aware expert placements.

The main metrics are network-only latency, network energy, the final estimated
system cost after replacing proxy network costs with topology-model costs, and
feedback-loop sensitivity. Unless otherwise stated, reported speedups compare
network-only latency.

\section{Results and Discussion}
\subsection{Topology Choice Dominates Network Latency}
Table~\ref{tab:summary} summarizes the completed JSON-backed runs. The
circulant topology wins every completed AccelForge-backed workload. On the
unbatched matmul sweep, it is consistently $2.29\times$ faster than the 3D
torus and about $3.05\times$ faster than the slowest topology. The direct
64-chip mapping sweep preserves the same ranking, which supports the
8-to-64-chip scaling shortcut used for the larger sweep.

\begin{table}[t]
\caption{Completed AccelForge-backed network-latency results.}
\label{tab:summary}
\centering
\small
\begin{tabular}{lcccc}
\hline
Run & Workloads & Circ. wins & vs. Torus & vs. worst \\
\hline
Unbatched matmul & 7 & 7 & $2.29\times$ avg. & $3.05\times$ avg. \\
Batched matmul & 21 & 21 & $2.17\times$ avg. & $3.04\times$ avg. \\
GPT-3 175B & 1 & 1 & $1.80\times$ & $3.03\times$ \\
64-chip matmul & 4 & 4 & $2.09\times$ avg. & $3.04\times$ avg. \\
\hline
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{matmul_latency_comparison.png}
\caption{Network latency for the current unbatched matmul sweep. The circulant
graph wins all seven workloads; the torus is consistently second for these
reduction-heavy mappings.}
\label{fig:matmul-latency}
\end{figure}

Figure~\ref{fig:matmul-latency} shows that the gap is stable over several
orders of magnitude in message size. For example, on the wide
$8\mathrm{K}\times256\mathrm{K}$ workload, network latency is 2004 ms on the
circulant, 4581 ms on the torus, 6013 ms on the ring, and 6108 ms on the mesh.
The mesh and ring lose for different reasons: the mesh has more links than the
ring but lacks wraparound ring collectives, while the ring has very low degree
and therefore a large bottleneck load.

\subsection{Batched Workloads Preserve the Bottleneck Pattern}
The batched sweep increases traffic volume without changing the key bottleneck
mechanism. The circulant wins all 21 batched workloads. Latency scales almost
linearly with batch size when the collective plan is unchanged, confirming that
serialization, not first-byte latency, dominates the modeled costs. The
smallest batched workload is the main exception because its mapping introduces
three transfers, including broadcasts, but the circulant remains fastest.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{batched_batch_scaling.png}
\caption{Batched matmul bottleneck serialization scales with batch size. This
supports the conclusion that the topology advantage comes from max-link load
rather than fixed hop latency.}
\label{fig:batched}
\end{figure}

\subsection{GPT-3 and Transformer Stress Tests}
The decomposed GPT-3 175B run produces 1344 transfers and
$1.66\times10^{14}$ bytes of modeled network traffic. The circulant completes
the network replay in $2.42\times10^6$ ms, compared with
$4.34\times10^6$ ms for the torus, $5.41\times10^6$ ms for the ring, and
$7.33\times10^6$ ms for the mesh. The traffic mix is not only AllReduce:
by bytes, the circulant run is 48\% AllReduce, 25\% Broadcast, 24\% AllGather,
and 3\% ReduceScatter. This explains why the GPT-3 speedup over torus is lower
than the pure AllReduce prediction: broadcast and AllGather do not benefit from
the Hamiltonian-ring schedule in the same way.

The direct transformer stress tests, shown in
Fig.~\ref{fig:transformer-stress}, broaden this beyond the single decomposed
GPT-3 run. They include decode, prefill, gradient synchronization, and pipeline
activation traffic. The circulant is most valuable in bandwidth-heavy collective
regimes; its advantage shrinks in tiny-message cases where the fixed hop term
becomes visible.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{transformer_circulant_vs_torus.png}
\caption{Direct transformer stress tests comparing the circulant graph to the
3D torus over decode, prefill, gradient synchronization, and pipeline traffic.}
\label{fig:transformer-stress}
\end{figure}

\subsection{MoE Dispatch and Placement}
MoE layers stress a different communication pattern: sparse token dispatch to
expert chips followed by output combine. We model these as two phase-synchronous
sets of point-to-point transfers, so link loads are merged within a phase.
Figure~\ref{fig:moe} compares topology and placement choices. This experiment
shows that topology-aware expert placement is complementary to topology choice:
a better graph reduces path and bottleneck costs, while a better placement
keeps local or high-volume token flows near their experts.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{moe_latency_by_topology.png}
\caption{Synthetic MoE dispatch/combine latency across topologies and expert
placements. This workload stresses sparse all-to-all traffic rather than dense
full-chip collectives.}
\label{fig:moe}
\end{figure}

\subsection{Energy and Mapping Sensitivity}
Network energy follows total bit-hops rather than bottleneck load. This creates
one useful contrast: the ring often has energy close to the circulant because it
uses adjacent hops, but it has much worse latency because only two links per
chip carry the ring collective. In the unbatched matmul sweep, average network
energy is 422 J for the circulant and ring, 965 J for the torus, and 1930 J for
the mesh; average network latency is 1.53 s, 4.58 s, 3.49 s, and 4.65 s,
respectively.

The feedback-loop results answer the mapping-sensitivity question from the
project prompt. The actual network model substantially increases the estimated
cost relative to the initial proxy model, but the collective plan is stable:
all successful matmul, batched matmul, GPT-3, and 64-chip matmul runs converge
in two iterations, and the first-to-final inferred collective plan does not
change. Thus, for these workloads, topology mostly changes the cost of a chosen
partitioning rather than causing AccelForge to select a different communication
structure.

\subsection{Pending Tall-Matmul Sweep}
A tall-matmul sweep comparing \texttt{MAP\_CHIPS=8} and
\texttt{MAP\_CHIPS=64} over batch size, $N$, and $K$ is scaffolded but does not
yet have aggregate results in the repository. The final report should reserve a
small heatmap or table here showing whether tall activation-heavy shapes change
the collective plan or weaken the 8-chip mapping approximation.

\section{Conclusion}
The experiments support the central claim: under the same six-link-per-chip
budget as a TPU-v4-like 3D torus, a Hamiltonian-decomposable circulant graph is
a better fit for the collective patterns generated by our distributed inference
workloads. Its advantage is not due to adding hardware links; it comes from
spreading large reductions over edge-disjoint full-chip cycles and reducing the
bottleneck-link load. Across completed AccelForge-backed experiments, the
circulant wins every workload, while feedback analysis shows that the mapping
usually stabilizes quickly and does not change collective structure after
network costs are fed back.

\clearpage
% The LLM Usage and References sections do not count toward the page limit.

\section{LLM Usage}
\textcolor{red}{We used an LLM to help summarize repository artifacts, draft report prose, and
turn experiment logs into concise tables and figure captions. It was most
helpful for synthesizing results across many JSON files and keeping the report
organized under the page limit. It was less helpful for validating numerical
claims; those were checked directly against scripts and result files.}

\begin{thebibliography}{00}
\bibitem{tpuv4}
N. Jouppi et al., ``TPU v4: An Optically Reconfigurable Supercomputer for
Machine Learning with Hardware Support for Embeddings,'' \emph{ISCA}, 2023.

\bibitem{megatron}
M. Shoeybi et al., ``Megatron-LM: Training Multi-Billion Parameter Language
Models Using Model Parallelism,'' arXiv:1909.08053, 2019.

\bibitem{gshard}
D. Lepikhin et al., ``GShard: Scaling Giant Models with Conditional
Computation and Automatic Sharding,'' \emph{ICLR}, 2021.

\bibitem{switch}
W. Fedus, B. Zoph, and N. Shazeer, ``Switch Transformers: Scaling to Trillion
Parameter Models with Simple and Efficient Sparsity,'' \emph{JMLR}, 2022.

\bibitem{circulant_survey}
F. K. Hwang, ``A Survey on Multi-Loop Networks,'' \emph{Journal of Research of
the National Bureau of Standards}, vol. 85, no. 4, 1980.
\end{thebibliography}

\end{document}
